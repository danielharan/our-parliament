<li>
  <%= link_to image_tag('senator.jpg', :alt => senator.name), senator_path(senator), :class => 'photo' %>
  <div class="who">
    <h3 class="name"><%= link_to senator.name, senator_path(senator) %></h3>
    <span class="meta">
      (<span class="affiliation"><%= senator.affiliation %></span>,
      <span class="province"><%= senator.province %></span>)
    </span>
  </div>
</li>